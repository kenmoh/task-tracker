import React from 'react';

const NoTask = () => {
    return (
        <div style={{ backgroundColor: 'black', color: 'white', padding: '5px', fontWeight: 'bold', textAlign: 'center' }}>
            <p>No task to show!</p>
        </div>
    );
};

export default NoTask;
